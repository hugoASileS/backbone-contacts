import { Alert, AlertTitle } from "@mui/material";
import * as React from "react";

export default function SuccessHttp() {
  return (
    <Alert>
      <AlertTitle>Success</AlertTitle>
      the information was saved successfully
    </Alert>
  );
}
